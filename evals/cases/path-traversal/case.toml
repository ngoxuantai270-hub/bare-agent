task = "Fix the report storage security issue while preserving valid nested paths. Do not modify tests, and verify the fix."
timeout_seconds = 180
verify_argv = ["python", "-m", "pytest", "-q", "tests", "_hidden_tests"]
protected_globs = ["tests/**/*.py"]
